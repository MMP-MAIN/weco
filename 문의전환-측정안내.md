# 문의 전환 측정 — 2026-09-24

변경 범위: 모바일 상담 문의 강조, 문의 폼 간격 축소, 문의 단계 측정.
프로젝트 내용·사진·연결, 검색 글과 광고 설정은 변경하지 않음.

## 구분해서 볼 지표

| 이벤트 | 의미 | 주의 |
| --- | --- | --- |
| `view_contact` | 문의 영역 노출 | 기존 지표 |
| `form_start` | 기존 입력칸 포커스 지표 또는 GA 자동 측정 | 실제 입력 수로 해석하지 않음 |
| `inquiry_input_start` | 이름·연락처·문의·예산 중 공백이 아닌 값이 처음 확인됨 | 입력/변경 또는 제출 시 확인. 입력 내용은 전송하지 않음 |
| `inquiry_submit_attempt` | 제출 시도 | 필수 항목 오류도 포함. 한 번에 여러 필드가 잘못돼도 시도는 한 번 |
| `inquiry_validation_error` | 필수 항목 등 브라우저/클라이언트 검증 오류 | 고정된 항목 이름과 오류 종류만 전송. 한 시도에 여러 오류 가능 |
| `form_submit_error` | 서버 응답 실패·네트워크 오류·시간 초과 | 오류 종류만 전송. 입력 내용은 유지 |
| `generate_lead` | 접수 서비스가 성공 응답 | 실제 메일 도착·유효 고객·계약 성사와는 별개 |

`attempt_number`는 현재 페이지 안에서 증가하는 시도 순서다. 방문자 식별자나 접수번호가 아니다.
가이드 경유 문의는 기존 `inquiry_topic`, `inquiry_guide`를 함께 전달한다.
이름·전화번호·문의 내용·예산 값·브라우저 오류 원문은 신규 이벤트에 담지 않는다.
전송 중 중복 제출을 차단하고, 분석 전송 실패가 문의 처리를 막지 않게 한다.

## 보고서 해석

- 배포 이후의 데이터로 비교한다. 과거 실제 입력·시도 수는 소급해서 복원할 수 없다.
- 실제 문의 전환은 같은 방문자의 단계 순서로 분석한다. 서로 다른 이벤트의 사용자 수를 단순히 나눠 순차 전환율로 단정하지 않는다.
- 신규 이벤트를 모두 핵심 이벤트로 지정하지 않는다. 입력·시도·카카오 클릭은 DB가 아니다.
- 최종 월 DB는 수신 문의를 대조하고 테스트·스팸·중복을 제외한 연락 가능한 신규 문의로 집계한다.
- 보고서 속성의 사용자 정의 측정기준/탐색 보고서 설정은 이 코드 변경에 포함하지 않았다.
- 자동 테스트는 모의 전송만 사용하며 실제 이메일이나 문의를 보내지 않는다.
